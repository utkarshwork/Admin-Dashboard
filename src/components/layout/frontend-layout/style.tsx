import { Box, styled } from "@mui/material";

export const StyledParentBox = styled(Box)(() => ({
  width: "100%",
  maxWidth: "768px",
  margin: "0 auto",
  fontFamily: "inherit",
  minHeight: "100vh",
}));
